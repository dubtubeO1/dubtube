[phases.setup]
nixPkgs = ["nodejs", "npm", "python3", "pip", "ffmpeg"]

[phases.install]
cmds = [
  "pip install yt-dlp",
  "npm install"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
