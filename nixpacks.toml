[phases.setup]
nixPkgs = ["nodejs_18", "python311", "ffmpeg", "yt-dlp"]

[phases.install]
cmds = [
  "npm install"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"