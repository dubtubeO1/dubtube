[phases.setup]
nixPkgs = ["nodejs_18", "python311", "ffmpeg"]

[phases.install]
cmds = [
  "pip install yt-dlp",
  "npm install"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"